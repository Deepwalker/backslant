- from . import base
- from .lib import input

:call base.render(**options)
    :content
        h2
            | Index page
        div
            - for i in range(10):
                p
                    - yield "Paragraph {}".format(i)
        custom-tag#identifier
        :call input('name', value='Me!')
